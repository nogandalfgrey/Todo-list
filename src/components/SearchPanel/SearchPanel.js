import React from 'react';

import './SearchPanel.css'

const SearchPanel = () => {

  return (
      <input className='form-control search-panel' type="search" placeholder="search"/>
  )
};

export default SearchPanel;